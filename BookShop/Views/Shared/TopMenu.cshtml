@model List<Model.EF.Manu>

    <p class="right">
        @foreach(var item in Model)
        {
           <a href="@item.Link" target="@item.Target">@item.Text</a>
            <a>|</a>
        }
        
    </p>
